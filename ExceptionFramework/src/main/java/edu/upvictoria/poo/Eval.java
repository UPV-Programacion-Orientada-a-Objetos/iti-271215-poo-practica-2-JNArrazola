package edu.upvictoria.poo;

/**
 * Fundamental class to do the evaluation
  */
public class Eval {
    
    /**
     * Function to evaluate the query
     * @param query
     */
    public static void eval(String query){
        if(query.toUpperCase().startsWith("ROUND")){
            
        }
    }
}
